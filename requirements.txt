# Web
Flask>=3.0
Flask-SQLAlchemy>=3.1
Flask-Login>=0.6.3
gunicorn>=21.2 ; platform_system != "Windows"
waitress>=3.0 ; platform_system == "Windows"

# Audio
librosa>=0.10.2
soundfile>=0.12
soxr>=0.3
noisereduce>=3.0
scipy>=1.11

# Machine learning
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
xgboost>=2.0
joblib>=1.3

# Visualisation / reports
matplotlib>=3.8
openpyxl>=3.1

# Testing
pytest>=8.0

# Optional helpers (dataset collection)
# sounddevice>=0.4   # scripts/record_samples.py
# pyttsx3>=2.90      # scripts/generate_help_phrases_tts.py

# Optional – YAMNet features (feature set v2). Without them the model is trained without YAMNet
# (python -m python_models.train_models --no-yamnet) and the web app does not need TensorFlow.
# tensorflow>=2.15
# (tensorflow-hub is NOT needed: YAMNet is loaded with tf.saved_model.load)
