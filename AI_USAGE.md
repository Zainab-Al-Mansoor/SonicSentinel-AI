# AI tool usage declaration

The SRS requires every AI tool used during development to be declared here. **Every team member must review, understand, modify where needed, and test all AI-assisted code.** Evaluators may ask any member to explain any function.

The final sound classification is produced **only** by the team's Python model and the team's Google Teachable Machine model. No generative-AI API is called by the application.

| # | Tool | Purpose | Assistance requested | Files affected | Student modifications | Testing completed | Verified by |
|---|---|---|---|---|---|---|---|
| 1 | Claude (Anthropic) | Initial project scaffold | Generate the first version of the Flask app, audio pipeline, training scripts, tests and docs from the SRS | whole repository (initial commit) | Reviewed the generated structure module by module, gave the project its name and branding (our own logo artwork), set default settings and ran every page in the browser before building on it | `python -m pytest` (55 tests), manual browser test | Zainab |
| 2 | Claude (Anthropic) | Importing our downloaded datasets | Script that imports the local `downloads/` folders with label mapping, per-class and per-source caps and round-robin selection | `scripts/import_local_downloads.py`, `config/dataset_mapping.json` | Downloaded the Glass Breaking and other extra clips ourselves, arranged the `downloads/` folders, and chose the caps (e.g. `--max-per-class 5000` for Background Noise) | import run on our PC; clip counts checked in `data/dataset_statistics.json` | Zainab |
| 3 | Claude (Anthropic) | Training fixes | Skip XGBoost when a class has no data; feature-cache key with size + mtime; de-duplicate annotations; augmentation balancing (`--balance-to`); dataset clean-up script | `python_models/train_models.py`, `scripts/build_dataset.py`, `augmentation/augment.py`, `scripts/clean_dataset.py` | Ran the full pipeline on our PC several times, chose `--fast` with rf, mlp and xgb, stopped a run that was missing the augmented clips and restarted it after augmentation | full rebuild → augment → train on our PC (final model: 19,424 training clips, XGB 93.4 % test accuracy, macro-F1 0.945) | Zainab |
| 4 | Claude (Anthropic) | Explaining GTM and debugging | Step-by-step Teachable Machine guidance, why GTM predictions were wrong, how to retrain | `gtm_model/README.md`, `documentation/GTM_TRAINING_LOG.md` | Trained the GTM models ourselves in the browser (4 runs), uploaded the class archives, solved the upload and training errors step by step and exported run 4 (2026-09-26) into `gtm_model/model/` | GTM model exported and tested in the app | Zainab |
| 5 | Claude (Anthropic) | UI theme | Mocha colour theme (#3E2522, #8C6E63, #D3A376, #FFE0B2, #FFF2DF) and glass effect; plot colours | `static/css/app.css`, `templates/base.html`, `static/img/logo.svg`, several templates, `feature_extraction/visuals.py` | Chose the colour palette; this theme was later replaced by our Aura theme (row 14) | every page checked on desktop and mobile width | Zainab |
| 6 | Claude (Anthropic) | Recording helper | Microphone device list / selection and level check in the recording script | `scripts/record_samples.py` | Used the device list to pick the correct microphone input; script logic unchanged | recording levels checked with the level meter | Zainab |
| 7 | Claude (Anthropic) | Performance evidence and deployment | Benchmark script for the NFR targets; Dockerfile, Render blueprint and deployment guide | `scripts/benchmark_performance.py`, `Dockerfile`, `.dockerignore`, `render.yaml`, `documentation/DEPLOYMENT.md` | Ran the benchmark on our PC; chose the deployment option for our model size | benchmark run on our PC; deployment tested on Render | Zainab |
| 8 | Claude (Anthropic) | Documentation drafts | Drafts of README, project report, technical blog, demo-video script, SRS compliance checklist, submission checklist, development log | `README.md`, `documentation/*.md`, `DEVELOPMENT_LOG.md`, `screenshots/README.md` | Read every document, corrected facts that did not match our work and are updating the numbers to the final model (93.4 %) in our own words | numbers checked against `reports/` | Zainab |
| 9 | Claude (Anthropic) | GTM upload archives, availability, benchmark evidence | Teachable Machine sample-archive export (`--zip`), `/healthz` health check, auto-restart server script, tests | `gtm_model/prepare_gtm_samples.py`, `gtm_model/README.md`, `src/__init__.py`, `run_server.bat`, `tests/test_gtm_samples.py`, `tests/test_app.py`, `reports/performance.md` | Generated the archives with `--max-per-class`, uploaded them to Teachable Machine ourselves and checked `/healthz` while the server was running | 58 pytest tests; archives accepted by the Teachable Machine web app; benchmark with the real model | Zainab |
| 10 | Claude (Anthropic) | Standout features | "Why this prediction?" explainable-AI panel (TreeSHAP / occlusion) and Robustness Lab with live sliders and stress test | `src/services/explain.py`, `src/services/lab.py`, `src/routes/api.py`, `src/routes/main.py`, `templates/events/detail.html`, `templates/lab.html`, `templates/base.html`, `static/js/lab.js`, `static/css/app.css`, `tests/test_explain_lab.py` | Tested the panel on our own uploads and used the Lab sliders to check how both models react to noise and echo | 66 pytest tests; checked in the browser with the real Python and GTM models | Zainab |
| 11 | Claude (Anthropic) | User and developer guides | Word/PDF user guide and developer guide with diagrams, charts and page references | `documentation/SonicSentinel_AI_User_Guide.docx`, `documentation/SonicSentinel_AI_Developer_Guide.docx` / `.pdf` | Read both guides, asked for more professional layout and checked the steps against the running app | opened in Word, every step followed once in the app | Zainab |
| 12 | Claude (Anthropic) | Data cleaning and label audit | Glass look-alike clean-up script; out-of-fold label audit with an HTML listening page; script that applies the decisions | `scripts/clean_glass_extra.py`, `scripts/label_audit.py`, `scripts/apply_label_fixes.py` | Listened to the flagged clips ourselves in Chrome and decided keep / move / delete for each one; did not bulk-delete hard examples | dry run first, then `--apply`; dataset rebuilt (6,938 clips) | Zainab |
| 13 | Claude (Anthropic) | Feature set v2 | 94 extra hand-crafted features, context features, YAMNet embeddings, class weights, per-class calibration and a feature-ablation report | `feature_extraction/extra.py`, `embeddings.py`, `pipeline.py`, `python_models/train_models.py`, `python_models/inference.py`, `src/services/analysis.py`, `lab.py`, `explain.py`, `tests/test_features_v2.py` | Chose the "all steps first, train once" plan, installed TensorFlow, fixed the environment (setuptools) and ran the final training overnight | 76 pytest tests; final model 93.4 % test accuracy; `reports/feature_ablation.csv` | Zainab |
| 14 | Claude (Anthropic) | Aura UI theme and animations | Dark-purple theme from our palette, animated radar/waveform banner, scroll and count-up animations, silver logo, dark plot colours | `static/css/app.css`, `static/js/motion.js`, `templates/base.html`, `templates/dashboard.html`, `templates/partials/hero_radar.html`, `static/img/*.svg`, `feature_extraction/visuals.py` | Gave the colour palette and reference design, and changed the text colours ourselves to make them brighter | every page checked in Chrome at different zoom levels | Zainab |
| 15 | Claude (Anthropic) | UI bug fixes | Navbar breaking into two lines at 100 % zoom; review table disappearing; animated login radar | `templates/base.html`, `static/css/app.css`, `static/js/motion.js`, `static/img/auth-art.svg` | Found and reported both bugs with screenshots and re-tested after the fix | checked on our laptop screens after Ctrl+F5 | Zainab |
| 16 | | | | | | | |

## Notes for the team

* Keep adding a row whenever you use an AI tool (ChatGPT, Copilot, Claude, …).
* In "Student modifications", be specific, e.g. "tuned SVM grid, rewrote quality thresholds after testing on our recordings".
* The report and blog are drafts: read them, correct anything that does not match what you did, and add your own experience before submitting.
* Before the demo, every member should be able to explain their assigned modules. Suggested split:
  * `audio_preprocessing/` and `feature_extraction/`
  * `python_models/`, `augmentation/` and `scripts/build_dataset.py`
  * `gtm_model/` and `static/js/gtm.js`
  * `src/services/decision.py`, `rules.py` and `analysis.py`
  * `src/routes/`, `templates/` and `tests/`
